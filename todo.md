# Todos
[ ] Layer focus.   Overrides the z-ordering for input by giving the focussed layer upmost input priority (Not rendering priority).
                   IME layer still gets input first however.
[ ] Timer signals. A timing signal that layers can request to be send at a certain time.
                   The signal should have the layer associated with it stored, so that the layers know who this timing signal is for.
